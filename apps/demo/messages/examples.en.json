{"built_with":"Built with Floating UI Plus","implemented_with":"implementation","example_tooltip_build":"We combined floating-root with hover(), focus(), dismiss(), role(), safePolygon(), and arrow middleware to build contextual help that works for both pointer and keyboard users.","example_popover_build":"We composed floating-root, floating-portal, click dismissal, and offset/flip/shift middleware to build an anchored details panel that remains connected after it moves in the DOM.","example_menu_build":"We used floating-list with navigation, typeahead, and loop plus one collection registry to build a command navigator with dependable keyboard movement.","example_nested_menu_build":"We coordinated floating-tree, floating-node, nested floating-list, and descendant dismissal to build a command hierarchy whose submenus retain the right parent focus.","example_client_point_build":"We combined clientPoint(), hover({move: true}), and a virtual reference with flip and shift middleware to build context that follows a precise pointer position.","example_combobox_build":"We composed SearchController, createFuzzySearchSource(), listNavigation(), role(), and floating-portal to build a multilingual destination picker without tying search to one UI framework.","example_placement_build":"We used the typed Placement surface and offset middleware to build a placement lab where one product decision immediately updates the floating surface.","example_middleware_build":"We applied the same floating-root primitive to isolated offset, shift, flip, arrow, size, autoPlacement, hide, and inline experiments so each layout constraint is visible in action.","example_modal_build":"We composed floating-tree, floating-node, floating-portal, floating-overlay, and floating-focus-manager to build layered dialogs with inert surroundings, scroll locking, return focus, and ordered Escape dismissal."}
